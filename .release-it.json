{
  "git": {
    "commitMessage": "${version}",
    "tagName": "v${version}",
    "addUntrackedFiles": true
  },
  "github": {
    "release": true,
    "releaseName": "${version}"
  },
  "hooks": {
    "before:init": ["npm test"],
    "before:bump": "npm run build"
  }
}
